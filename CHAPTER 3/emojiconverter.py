# Emoji Converter- basic version

message=input("Enter your message: ") # message- Bright stars shimmer gently above, whispering secrets to the night:).

message= message.replace(":)","😊")
message= message.replace(":(","😞")
message= message.replace(":D","😃")
message= message.replace(";)", "😉")

print(message) #output - Bright stars shimmer gently above, whispering secrets to the night😊.